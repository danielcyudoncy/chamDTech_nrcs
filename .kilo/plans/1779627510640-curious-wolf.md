# Fix 17 Dart Analysis Errors

## Problem Summary
The codebase has 17 errors:
- 4 errors: Missing import for `TapGestureRecognizer` in login_screen.dart and signup_screen.dart
- 13 errors: `.withValues()` method not available (needs to be replaced with `.withOpacity()`)

## Solution

### Fix 1: Add missing import to login_screen.dart (line 1-6)
Add `import 'package:flutter/gestures.dart';` after the existing imports.

### Fix 2: Add missing import to signup_screen.dart (line 1-5)
Add `import 'package:flutter/gestures.dart';` after the existing imports.

### Fix 3: Replace `.withValues()` with `.withOpacity()` (13 occurrences)
The `.withValues(alpha: value)` method is not available in older Flutter SDK versions. Replace all occurrences with `.withOpacity(value)`:

Files to update:
- lib/features/auth/views/login_screen.dart (lines 417, 438)
- lib/features/auth/views/signup_screen.dart (lines 583, 594)
- lib/features/auth/controllers/auth_controller.dart (lines 45, 76)
- lib/features/auth/services/auth_service.dart (lines 88, 115, 124, 171, 180)

## Implementation Steps
1. Edit login_screen.dart: Add gestures import, replace 2 withValues calls
2. Edit signup_screen.dart: Add gestures import, replace 2 withValues calls
3. Edit auth_controller.dart: Replace 2 withValues calls
4. Edit auth_service.dart: Replace 5 withValues calls

## Notes
- The remaining withValues occurrences in other files are not part of the reported 17 errors
- This fix maintains backward compatibility with older Flutter SDK versions